## Project Structure

This project uses a `src/`-based layout:

- `src/forecast_lib/`: core library code
- `tests/`: unit tests
# my-forecast-lib
reset practice line

## Overview
Forecasting library scaffold built during Week 1 Git fundamentals.
